<p id="notice"><%= notice %></p>

	

	<p>
   		<%=form_tag cars_path, id:'search', remote: true, method: :get do %>
    	<%= text_field_tag :search, params[:search]  %>
    	<%= submit_tag "Search", :name => nil,:value => :search.upcase %>
	</p>
    <% end %>

	

<div id="search"><%= render 'search' %></div>
